# Dependencies
**/node_modules

# OS
.DS_Store
Thumbs.db

# Environment files
.env
.env.*
!.env.example

# Audit reports and screenshots (local only, not part of the project)
/Audit/

# Claude Code configuration and agents (local only)
.claude/*
!.claude/skills/
.claude/skills/*
# Project-owned skills are versioned; the rest of .claude/skills are symlinks
# into .agents/skills and stay local.
!.claude/skills/mealmap-design-system/

# Third-party design skills: reproducible from skills-lock.json, not versioned
/.agents/skills/

# Nested local harness repo (own git history)
/mealplanner-harness/
